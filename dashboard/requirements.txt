# What `streamlit run dashboard/app.py` needs locally. The published Space installs
# nothing: it is static HTML built by scripts/build_static_space.py, so no dependency in
# this file reaches a reader. Kept small anyway, because the development renderer reads the
# same committed Parquet and JSON and so needs no DuckDB, no torch, and no EIA client.
streamlit>=1.40.0
altair>=5.4.0
pandas>=2.2.0
pyarrow>=17.0.0
